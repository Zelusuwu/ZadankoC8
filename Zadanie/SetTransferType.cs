using System;
using System.Collections.Generic;
using System.Text;

namespace Zadanie
{
    class SetTransferType
    {
        protected Send _send;
        public void SetTransfer(Send send)
        {
            _send = send;
        }
    }
}
